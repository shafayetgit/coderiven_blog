import { LinearProgress } from '@mui/material'

const CLinearProgress = () => {
  return (
    <LinearProgress sx={{ position: "fixed", top: 0, left: 0, width: "100%", zIndex: 1200 }} />
  )
}

export default CLinearProgress